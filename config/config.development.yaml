server:
  port: "8080"    # Default port

database:
  host: "localhost"
  port: 5432
  name: "mydb_devel"

sqlite:
  route: "./db/development.db"  # SQLite database file path

jwt:
  publickey_path: "config/keys/public.pem"
  privatekey_path: "config/keys/private.pem"